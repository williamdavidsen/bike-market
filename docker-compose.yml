services:
  postgres:
    image: postgres:16-alpine
    container_name: bikemarket-postgres
    restart: unless-stopped
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-bikemarket}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-bikemarket_password}
      POSTGRES_DB: ${POSTGRES_DB:-bikemarket}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-bikemarket} -d ${POSTGRES_DB:-bikemarket}"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  postgres_data:
