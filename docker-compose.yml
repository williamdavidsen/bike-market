services:
  postgres:
    image: postgres:16-alpine
    container_name: sykkelix-postgres
    restart: unless-stopped
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-sykkelix}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-sykkelix_password}
      POSTGRES_DB: ${POSTGRES_DB:-sykkelix}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-sykkelix} -d ${POSTGRES_DB:-sykkelix}"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  postgres_data:
